name: Backup

on:
  pull_request:
  schedule:
    - cron: "0 2 * * *" # daily @ 02:00

jobs:
  export:
    runs-on: ubuntu-latest

    services:
      mssql:
        image: mcr.microsoft.com/mssql/server:2022-latest
        env:
          ACCEPT_EULA: Y
          MSSQL_SA_PASSWORD: hunter2
        ports:
          - 1433:1433
        volumes:
          - /tmp/storage:/data

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version
          bundler-cache: true

      - name: Download
        run: aria2c -x 5 https://ODAwebpublish:b56ff26a-c19b-4322-a3c4-614de155781d@oda.ft.dk/odapublish/oda.bak

      - name: Restore
        run: /opt/mssql-tools/bin/sqlcmd -S mssql -U SA -P hunter2 -Q "RESTORE DATABASE oda FROM DISK = '/data/oda.bak' WITH MOVE 'ODA' TO '/var/opt/mssql/data/ODA.mdf', MOVE 'ODA_log' TO '/var/opt/mssql/data/ODA_log.ldf'"

      - name: Dependencies
        run: gem install tiny_tds

      - name: Convert ft export into zipped json
        run: bin/rails backup:export

      - name: Create release on GitHub
        run: gh release create "v$(date +%Y.%m.%d)" './tmp/storage/export.tar.gz#Daily JSON export' --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
